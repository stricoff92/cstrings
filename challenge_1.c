
/*
    CHALLENGE 1
    say hello with a string literal

    Task:
      Use the printf function to display "hello world" on your terminal;

    Example:
      printf("welcome to alma!\n");


    Note:
      String literals are wrapped with double quotes.
      Statements end with semi-colon;
      The \n character displays as a new line on the terminal
      To compile and run your c program:

    $
    gcc challenge_1.c && ./a.out
*/

#include <stdio.h>

int main() {
  // Edit code below v v

  // Edit code above ^ ^
  return 0;
}