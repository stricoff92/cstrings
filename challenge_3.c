
/*
    CHALLENGE 3
    display your initials

    Task:
      Use  character variables variable to hold your initials.
      Print your initials to the terminal using printf and a formatted string;

    Example:
      
      char letterBall = 'b';
      char numberBall = '3';
      printf("next is %c %c\n", letterBall, numberBall);


    Note:
      Use the char type to declare a character variable.
      Use %c to display a character within a formatted string.
      printf accepts n parameters. They will appear in order within the formatted string.
      

    $
    gcc challenge_3.c && ./a.out
  
  
*/

#include <stdio.h>




int main() {
  // Edit code below v v


  // Edit code above ^ ^
  return 0;
}